[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resolve-node-kit"
version = "0.1.0.dev0"
description = "Node workflow tools for DaVinci Resolve Fusion and Color"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
